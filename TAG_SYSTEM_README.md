# 标签系统使用说明

## 📋 系统概述

标签系统已成功集成到博客平台中，支持文章和绘画作品的标签管理。标签采用多对多关系设计，支持标签复用和智能推荐。

## 🗄️ 数据库表结构

### 1. tags（标签表）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | VARCHAR(36) | 标签ID（主键，UUID） |
| name | VARCHAR(50) | 标签名称（唯一） |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

### 2. article_tags（文章-标签关联表）

| 字段 | 类型 | 说明 |
|------|------|------|
| article_id | VARCHAR(36) | 文章ID（外键） |
| tag_id | VARCHAR(36) | 标签ID（外键） |
| created_at | DATETIME | 关联创建时间 |

## 🚀 初始化步骤

### 1. 运行数据库迁移

```bash
npm run db:migrate:tags
```

这个命令会：
- 创建 `tags` 和 `article_tags` 两张表
- 插入 20 个常用标签（技术、生活、随笔、教程等）

## ✨ 功能特点

### 1. 智能标签输入组件（TagInput）

- **自动补全**：输入时自动匹配已有标签，显示下拉选项
- **快捷创建**：按空格或回车键自动创建新标签
- **防重复**：自动检测并防止添加重复标签
- **数量限制**：默认最多 10 个标签，可自定义
- **标签管理**：点击标签的 × 即可删除

### 2. 集成页面

标签功能已集成到以下三个页面：

#### 📝 发布文章页面（/publish-article）
- 位置：在文章标题和分类目录之间
- 使用 TagInput 组件
- 支持从已有标签中选择或创建新标签

#### 🎨 绘画发布页面（Gallery Publish Float）
- 位置：在分类选择之后
- 使用 TagInput 组件
- 发布绘画作品时可以添加标签

#### ✏️ 文章编辑页面（/article/[id]）
- 位置：编辑模式下，在文章目录之后
- 使用 TagInput 组件
- 支持编辑现有文章的标签
- 浏览模式下会显示文章的所有标签

## 📡 API 接口

### 1. 获取所有标签
```http
GET /api/tags
```

**响应示例：**
```json
{
  "success": true,
  "tags": [
    {
      "id": "tag-xxx",
      "name": "技术",
      "created_at": "2024-12-02T10:00:00.000Z",
      "updated_at": "2024-12-02T10:00:00.000Z"
    }
  ]
}
```

### 2. 创建新标签
```http
POST /api/tags
Content-Type: application/json

{
  "name": "新标签"
}
```

**响应示例：**
```json
{
  "success": true,
  "tag": {
    "id": "tag-xxx",
    "name": "新标签"
  },
  "message": "标签创建成功"
}
```

### 3. 发布/更新文章时包含标签

发布文章时，在请求体中添加 `tags` 字段：

```json
{
  "title": "文章标题",
  "blocks": [...],
  "tags": ["技术", "教程", "React"],
  "category_id": "cat_xxx"
}
```

### 4. 获取文章时返回标签

文章详情和列表接口都会自动返回标签信息：

```json
{
  "success": true,
  "article": {
    "id": "article-xxx",
    "title": "文章标题",
    "tags": ["技术", "教程", "React"],
    ...
  }
}
```

## 🎯 使用方法

### 在发布文章页面添加标签

1. 进入发布文章页面（/publish-article）
2. 找到"标签"输入框
3. 输入标签名称：
   - 如果匹配到已有标签，会显示下拉列表供选择
   - 没有匹配的标签，直接按**空格**或**回车**创建新标签
4. 已添加的标签会显示在输入框上方
5. 点击标签的 × 可以删除

### 在文章编辑模式添加/修改标签

1. 进入文章详情页（/article/[id]）
2. 点击右下角的编辑按钮进入编辑模式
3. 找到"文章标签"输入框
4. 按照上述方法添加或删除标签
5. 点击保存按钮保存修改

### 在绘画发布页面添加标签

1. 在照片墙页面点击右下角的"+"按钮
2. 填写标题、描述和选择分类
3. 在"标签"输入框中添加标签
4. 上传图片后点击发布

## 📊 数据流程

### 发布文章流程

```mermaid
用户输入标签
    ↓
TagInput 组件检查标签库
    ↓
匹配到 → 显示下拉选择
没匹配 → 按空格创建新标签
    ↓
提交文章
    ↓
API 处理：
  - 查找或创建标签
  - 建立文章-标签关联
    ↓
保存成功
```

### 查询文章流程

```mermaid
请求文章详情/列表
    ↓
查询 articles 表
    ↓
JOIN article_tags 和 tags
    ↓
返回文章 + 标签数组
```

## 🎨 组件使用示例

### TagInput 组件

```tsx
import TagInput from '@/app/components/TagInput';

function MyComponent() {
  const [tags, setTags] = useState<string[]>([]);

  return (
    <TagInput
      value={tags}
      onChange={setTags}
      placeholder="输入标签，按空格或回车添加"
      maxTags={10}
    />
  );
}
```

### 组件属性

| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| value | string[] | [] | 已选标签数组 |
| onChange | (tags: string[]) => void | - | 标签变化回调 |
| placeholder | string | "输入标签，按空格添加" | 输入框提示文字 |
| maxTags | number | 10 | 最多可添加的标签数量 |

## 🔧 技术实现

### 标签输入组件核心功能

1. **自动补全**：使用 antd 的 AutoComplete 组件
2. **实时搜索**：输入时过滤标签库，显示匹配项
3. **空格创建**：监听键盘事件，空格键触发创建
4. **防重复**：创建前检查是否已存在
5. **异步创建**：新标签自动调用 API 创建

### 数据库设计亮点

1. **多对多关系**：文章和标签通过中间表关联
2. **标签复用**：同一个标签可以关联多篇文章
3. **级联删除**：删除文章时自动删除关联关系
4. **索引优化**：为标签名称和关联表建立索引

## 📈 预设标签列表

系统预设了 20 个常用标签：

- **技术类**：技术、前端、后端、React、Node.js、TypeScript、JavaScript、CSS、HTML
- **教程类**：教程
- **生活类**：生活、随笔、思考、旅行、美食、音乐、电影
- **创作类**：设计、摄影、读书

## 🎯 最佳实践

1. **标签命名**：
   - 使用简短、准确的词汇
   - 避免过于具体或过于宽泛
   - 控制在 2-6 个字为宜

2. **标签数量**：
   - 每篇文章建议 3-5 个标签
   - 不超过 10 个标签

3. **标签选择**：
   - 优先使用已有标签
   - 新建标签前先搜索是否存在相似标签
   - 避免创建重复或近义的标签

## 🔍 后续优化建议

1. **标签统计**：显示每个标签的文章数量
2. **热门标签**：展示使用次数最多的标签
3. **标签搜索**：支持按标签筛选文章
4. **标签管理**：管理员可以合并、删除标签
5. **标签云**：在首页或侧边栏显示标签云

## ✅ 验证测试

测试步骤：

1. **创建标签**：
   ```bash
   npm run db:migrate:tags
   ```

2. **发布带标签的文章**：
   - 访问 `/publish-article`
   - 添加标签并发布

3. **查看文章标签**：
   - 访问文章详情页
   - 查看标签是否正确显示

4. **编辑标签**：
   - 进入编辑模式
   - 修改标签并保存

5. **标签自动补全**：
   - 在标签输入框中输入
   - 验证下拉框是否显示匹配标签

## 📝 总结

标签系统已完全集成到博客平台，具备以下特点：

✅ 完整的数据库设计（标签表 + 关联表）  
✅ 智能的标签输入组件  
✅ 自动补全和快捷创建  
✅ 三个页面全面支持  
✅ API 完整支持  
✅ 预设常用标签  
✅ 防重复和数量限制  

享受使用标签系统来更好地组织你的文章吧！🎉

