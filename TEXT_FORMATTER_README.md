# 📝 文字格式化工具说明

专为中文写作设计的排版工具，无需复杂的富文本编辑器，只提供最实用的格式化功能。

## 🎯 功能列表

### 1️⃣ 首行缩进

**功能：** 给每个自然段开头添加两个全角空格（　　）

**使用场景：** 中文文章排版规范，段首缩进两个字符

**示例：**
```
原文：
这是第一段。
这是第二段。

格式化后：
　　这是第一段。
　　这是第二段。
```

---

### 2️⃣ 移除缩进

**功能：** 去掉段首的全角空格和普通空格

**使用场景：** 网络复制的文字可能有各种缩进，统一去除

**示例：**
```
原文：
　　这是第一段。
    这是第二段。

格式化后：
这是第一段。
这是第二段。
```

---

### 3️⃣ 去除空行

**功能：** 删除连续的空行，保留单空行作为段落分隔

**使用场景：** 清理复制粘贴时产生的多余空行

**示例：**
```
原文：
第一段


第二段




第三段

格式化后：
第一段

第二段

第三段
```

---

### 4️⃣ 统一换行

**功能：** 规范段落间距，段落间保持单空行

**使用场景：** 统一文章的段落结构

**示例：**
```
原文：
第一段
第二段

第三段


第四段

格式化后：
第一段

第二段

第三段

第四段
```

---

### 5️⃣ 清理空格

**功能：** 删除多余的空格

**使用场景：** 清理复制粘贴或输入时产生的多余空格

**示例：**
```
原文：
这是一段    有多个空格的    文字。

格式化后：
这是一段 有多个空格的 文字。
```

---

### 6️⃣ 中文排版（一键） ⭐

**功能：** 一次性应用所有中文排版规则

**包含的操作：**
1. 统一标点为全角（中文标点）
2. 清理多余空格
3. 统一换行
4. 去除连续空行
5. 首行缩进

**使用场景：** 快速将文章转换为符合中文排版规范的格式

**示例：**
```
原文：
这是一段文字,里面有半角逗号.
还有一些    多余的空格.


这是第二段.

格式化后：
　　这是一段文字，里面有半角逗号。还有一些多余的空格。

　　这是第二段。
```

**标点转换规则：**
- `,` → `，` （逗号）
- `.` → `。` （句号）
- `;` → `；` （分号）
- `:` → `：` （冒号）
- `?` → `？` （问号）
- `!` → `！` （感叹号）
- `(` → `（` （左括号）
- `)` → `）` （右括号）
- `"` → `"` / `"` （引号智能配对）

---

### 7️⃣ 撤销

**功能：** 恢复上一次格式化之前的内容

**使用场景：** 格式化效果不满意，快速恢复

**说明：** 
- 每次格式化前会自动保存当前内容
- 支持撤销一次（可扩展为多步撤销）

---

## 🚀 使用方式

### 方式一：单个块格式化

1. 鼠标悬停在文字块上
2. 点击右上角的 `[格式化▼]` 按钮
3. 选择需要的格式化选项

```
┌─────────────────────────── [格式化▼] ─┐
│ 　　这是一段文字内容。                  │
│ 　　可以进行各种格式化操作。            │
└────────────────────────────────────────┘
```

### 方式二：批量格式化

1. 在文章内容区域顶部
2. 点击 `[批量格式化▼]` 按钮
3. 选择格式化选项
4. 将应用到所有文字块

```
🧱 文章内容  [3个块] [2文字]  [批量格式化▼]
```

---

## 💡 使用建议

### 推荐工作流程

1. **写作时**：专注内容，不必在意格式
2. **写完后**：使用"批量格式化"→"中文排版"一键美化
3. **微调**：个别块如果需要特殊处理，单独格式化
4. **不满意**：使用"撤销"恢复

### 常见场景

**场景1：从网络复制的文章**
```
问题：格式混乱，有各种奇怪的空格和换行
解决：批量格式化 → 中文排版
```

**场景2：纯文字输入**
```
问题：写完了，但没有缩进
解决：批量格式化 → 首行缩进
```

**场景3：英文标点**
```
问题：打字习惯用了半角标点
解决：批量格式化 → 中文排版（会自动转换标点）
```

**场景4：段落混乱**
```
问题：有的段落没换行，有的换了很多行
解决：批量格式化 → 统一换行
```

---

## 🔧 技术实现

### 核心函数

所有格式化函数位于 `app/utils/textFormatter.ts`：

```typescript
// 单一功能
addIndent(text)           // 首行缩进
removeIndent(text)        // 移除缩进
removeEmptyLines(text)    // 去除空行
normalizeBreaks(text)     // 统一换行
cleanSpaces(text)         // 清理空格
unifyPunctuation(text)    // 统一标点

// 组合功能
chineseFormat(text)       // 一键中文排版

// 通用接口
applyFormat(text, option) // 应用格式化
```

### 扩展开发

如果需要添加新的格式化功能：

1. 在 `textFormatter.ts` 中添加函数
2. 在 `FormatOption` 类型中添加选项
3. 在 `applyFormat` 函数中添加 case
4. 在组件中添加菜单项

---

## 🎨 设计理念

### 为什么不用富文本编辑器？

❌ **富文本编辑器的问题：**
- 功能过于复杂（字体、字号、颜色、对齐...）
- UI 臃肿，学习成本高
- 对中文写作不够友好
- 生成的 HTML 难以管理

✅ **我们的方案：**
- 只提供中文写作真正需要的功能
- UI 简洁，藏在下拉菜单中
- 纯文本存储，数据清晰
- 专注内容，格式化交给工具

### 核心原则

1. **简单优先**：只做最常用的功能
2. **中文优化**：针对中文排版规范
3. **批量操作**：一键应用到所有块
4. **可撤销**：不用担心格式化错误
5. **非侵入**：格式化功能藏在菜单中，不影响写作

---

## 📊 效果对比

### 原文（网络复制）
```
这是一段文字,里面有各种问题.比如标点是半角的,
还有多余的空格,


换行也很混乱.有时候一段话
被分成了
好几行.

这是第二段,也有同样的问题!
```

### 应用"中文排版"后
```
　　这是一段文字，里面有各种问题。比如标点是半角的，还有多余的空格，换行也很混乱。有时候一段话被分成了好几行。

　　这是第二段，也有同样的问题！
```

---

## 🔮 未来扩展

可以添加的功能：
- [ ] 英文单词首字母大写
- [ ] 数字格式统一（阿拉伯数字/中文数字）
- [ ] 自动添加书名号《》
- [ ] 多步撤销/重做
- [ ] 自定义格式化规则
- [ ] 格式化预设（学术论文、新闻稿、小说等）

---

## 💬 总结

这套文字格式化工具专门为中文写作设计，解决实际问题：

✅ 不需要复杂的富文本编辑器
✅ 不需要手动调整每个段落
✅ 一键应用中文排版规范
✅ 支持批量操作，效率高
✅ 可以撤销，不用担心

**让你专注写作，格式交给工具！** 🎉

