# 日记发布功能说明

## 功能概述

日记发布功能允许用户快速记录日常心情和感想，通过悬浮按钮快速发布日记。

## 使用方法

### 1. 发布日记

1. 访问归档页面（`/articles`）
2. 点击右下角的日记悬浮按钮（笔图标）
3. 在弹出的对话框中：
   - **日记内容**（必填）：记录今天的心情和感想，最多 500 字
   - **心情**：选择当前心情（默认：😊）
   - **天气**：选择天气状况（可选）
   - **地点**：记录所在位置（可选）
   - **分类**：选择日记分类（可选）
4. 点击"发布"按钮

### 2. 日记数据结构

日记作为 article 的一种特殊形式存储在数据库中：

- **type**: `'diary'` - 标识为日记类型
- **title**: 自动生成的日期时间（格式：`2025年12月03日 14:30`）
- **excerpt**: 存储日记正文内容
- **blocks**: 包含一个文本块，其中包含元信息（心情、天气、地点）和正文

### 3. 日记显示

日记在归档页面以 `DiaryCard` 的形式显示：

- **渐变背景**：黄色渐变卡片，易于识别
- **标题**：显示日期时间
- **心情**：右上角显示心情图标
- **内容预览**：显示前 4 行日记内容
- **底部信息**：显示天气和地点（如果有）

### 4. 技术细节

#### DiaryPublishFloat 组件

位置：`app/components/DiaryPublishFloat.tsx`

功能：
- 生成精确到分钟的日期标题
- 将心情、天气、地点等元信息整合到内容中
- 调用 `/api/articles` 接口创建日记

#### DiaryCard 组件

位置：`app/components/cards/DiaryCard.tsx`

功能：
- 从 `excerpt` 字段解析日记内容
- 提取元信息（心情、天气、地点）
- 优雅地展示日记卡片

#### 数据格式

日记内容在数据库中的存储格式：

```
心情：😊 | 天气：☀️ | 地点：北京

今天天气很好，心情也不错...
```

前端解析时会自动分离元信息和正文内容。

## 页面集成

- **文章归档页面** (`/articles`): 显示日记发布悬浮按钮，以卡片形式显示所有日记
- **文章详情页面** (`/article/[id]`): 查看完整日记内容

## 注意事项

1. 日记发布需要先登录
2. 日记内容最多 500 字
3. 标题自动生成，不需要手动输入
4. 日记默认发布状态为 `published`
5. 日记在归档页面可以按标签和关键词筛选

